// Headers

#include <stdio.h>

// entry point function 

void main(void)

{ 
      char cnum1 = 100;  // definition of cnum1 and store value = 100 
      char cnum2 = 5;    // definition fo cnum2 and store value = 5
      char char_result = 0;  // definition of char_result and store value = 0 
      char_result = cnum1 / cnum2; // division (Quotient) of cnum1 & cnum2 and store result in char_result 

      short snum1 = 80; // definition of snum1 and store value = 80 
      short snum2 = 10; // definition of snum2 and store value = 10 
      short short_result = 0; // definition of short_result and store value = 0 
      short_result = snum1 / snum2; // division (Quotient) of snum1 & snum2 and store result in short_result 

      int inum1 = 60; // definition of inum1 and store value = 60 
      int inum2 = 15; // definition of inum2 and store value = 15
      int int_result = 0; // definition of int_result and store value = 0 
      int_result = inum1 / inum2; // division(quotient) of inum1 &  inum2 and store result in int_result 

      float fnum1 = 60; // definition of fnum1 and store value = 60 
      float fnum2 = 15; // definition of fnum2 and store value = 15
      float float_result = 0; // definition of float_result and store value = 0 
      float_result = fnum1 / fnum2; // division (quotient) of fnum1 & fnum2 and store result in float_result 

      long lnum1 = 70;  // definition of lnum1 and store value = 70
      long lnum2 = 7;   // definition of lnum2 and store value = 7
      long long_result = 0; // definition of long_result and store value = 0 
      long_result = lnum1 * lnum2; // division(quotient) of lnum1 & lnum2 and store result in long_result

      double dnum1 = 105;  // definition of dnum1 and store value = 105
      double dnum2 = 105;  // definition of dnum2 and store value = 105
      double double_result = 0;  // definition of double_result and store value = 0 
      double_result = dnum1 / dnum2; // division(quotient) of dnum1 & dnum2 and store result in double_result 

}