// Headers
#include <stdio.h>

// Entry point function

void main(void)
{
    char cnum1  = 80;  // definition of cnum1 and store value = 80 
    char cnum2 = 8;    // definition of cnum2 and store value = 8
    char char_result = 0; // definition of char_result and store value = 0 
    char_result = cnum1 % cnum2; // division (Remainder) of cnum1 & cnum2 and store result in char_result 

    short snum1 = 100; // definition of snum1 and store value = 100 
    short snum2 = 10;  // definition of snum2 and store value = 10 
    short short_result = 0; // definition of short_result and store value = 0 
    short_result = snum1 % snum2; // division (remainder) of snum1 & snum2 and store result in short_result

    int inum1 = 70;  // definition of inum1 and store value = 70 
    int inum2 = 7;   // definition of inum2 and store value = 7
    int int_result = 0; // definition of int_result and store value = 0 
    int_result = inum1 % inum2; // division(remainder) of inum1 & inum2 and store result in int_result 

    float fnum1 = 60.05; // definition of fnum1 and store value = 60
    float fnum2 = 6.05;  // definition of fnum2 and store value = 6
    float float_result = 0; // definition of float_result and store value = 0
    float_result = fnum1 % fnum2; // division(remainder) of fnum1 & fnum2 and store result  in float_result 

    long lnum1 = 50; // definition of lnum1 and store value = 50 
    long lnum2 = 5; // definition of lnum2 and store value = 5
    long long_result = 0; // definition of long_result and store value = 0 
    long_result = lnum1 % lnum2; // division(remainder) of lnum1 & lnum2 and store result in long_result 

    double dnum1 = 40; // definition of dnum1 and store value = 40 
    double dnum2 = 4;  // definition of dnum2 and store value = 4 
    double double_result = 0; // definition of double_result and store value = 0 
    double_result = dnum1 % dnum2; // division(remainder) of dnum1 % dnum2 and store  result in double_result 


}