// Headers
#include <stdio.h>

// Entry point function 
int main()
{
	// initialization 
	int le = 2; 

	// termination condition 
	while(le <= 20)
	{
		printf("%d\n",le);
		
     // increment / decrement
		le += 2;
	}
	return 0; 
}