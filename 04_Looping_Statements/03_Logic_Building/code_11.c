// program to print the multiplication table of 5 using for loop 
// Headers
#include <stdio.h>

// Entry point function 
int main()
{
	for( int le = 1; le <= 10; le++)
	{
		printf(" 5 * %d = %d\n",le,5*le);
	}
	return 0; 
}