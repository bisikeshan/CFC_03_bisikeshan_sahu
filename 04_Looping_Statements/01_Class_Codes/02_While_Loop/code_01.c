// Headers
#include <stdio.h>

// Entry point function 

int main(void)
{
	// code
	int le = 1; // initialization

	// Terminating condition 
	while(le <= 10)
	{
		// loop body
		printf("le= %d\n",le);

		// Incremnt / Decrement
		++le; 
	}
	return 0; 
}