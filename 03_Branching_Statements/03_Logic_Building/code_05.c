// Headers
#include <stdio.h>

// Entry point function 
int main(void)
{
	  /* This program finds the  greatest of two given number
	  it compares the two numbers and prints the large one 
	  */
    
      int a = 10, b = 15; 

      if(a > b)
      {  
      	 printf("The greator number is %d\n",a);
      }
      else
      {
      	printf("The greator number is %d\n",b);
      }

       return 0; 
    
	
}